/**
 * Toast Service
 * Re-export Sonner toast for use outside React components
 *
 * Usage:
 *   import { toast } from '../services/toast.service'
 *   toast.success('✓ Réservation confirmée')
 */
export { toast } from 'sonner'
